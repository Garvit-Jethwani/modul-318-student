// ********RoostGPT********
// Test generated by RoostGPT for test csharp-coding-examples using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
using NUnit.Framework;
using System;
using System.Net.Http;
using System.Threading.Tasks;
using Newtonsoft.Json;
using SwissTransport.Core;
using SwissTransport.Models;
using SwissTransport.Core.Test;

namespace SwissTransport.Core.Test
{
    [TestFixture]
    public class Dispose_b753ff24a4
    {
        private Transport transport;

        [SetUp]
        public void SetUp()
        {
            transport = new Transport();
        }

        [Test]
        public void Dispose_DisposeHttpClientInstance_DoesNotThrowException()
        {
            // Arrange
            var httpClientField = typeof(Transport).GetField("httpClient", System.Reflection.BindingFlags.NonPublic | System.Reflection.BindingFlags.Instance);
            HttpClient httpClient = (HttpClient)httpClientField.GetValue(transport);

            // Act & Assert
            Assert.DoesNotThrow(() => transport.Dispose());
            
            // Clean-up - No additional cleanup needed as Dispose is called in the test
        }

        [Test]
        public void Dispose_TwiceCall_ShouldNotThrowException()
        {
            // Act & Assert
            Assert.DoesNotThrow(() => transport.Dispose());
            Assert.DoesNotThrow(() => transport.Dispose());
            
            // Clean-up - No additional cleanup needed as Dispose is called in the test
        }

        [TearDown]
        public void TearDown()
        {
            // Test fixture clean-up
            // Double-check to ensure resources are cleaned up in case tests fail before calling Dispose
            transport?.Dispose();
        }
    }
}

