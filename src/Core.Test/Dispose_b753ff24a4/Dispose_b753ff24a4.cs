// ********RoostGPT********
// Test generated by RoostGPT for test csharp-coding-examples using AI Type Open AI and AI Model gpt-4



// ********RoostGPT********
using NUnit.Framework;
using Moq;
using System;
using System.Net.Http;
using SwissTransport.Core;
using SwissTransport.Models;

namespace SwissTransport.Core.Test
{
    public class Dispose_b753ff24a4
    {
        private Mock<HttpClient> mockHttpClient;
        private Transport transport;

        [SetUp]
        public void SetUp()
        {
            mockHttpClient = new Mock<HttpClient>();
            transport = new Transport(mockHttpClient.Object);
        }

        [TearDown]
        public void TearDown()
        {
            transport.Dispose();
        }

        [Test]
        public void Dispose_HttpClientIsNotNull_HttpClientDisposeIsCalled()
        {
            // Arrange
            mockHttpClient.Setup(x => x.Dispose());

            // Act
            transport.Dispose();

            // Assert
            mockHttpClient.Verify(x => x.Dispose(), Times.Once);
        }

        [Test]
        public void Dispose_HttpClientIsNull_HttpClientDisposeIsNotCalled()
        {
            // Arrange
            transport = new Transport(null);

            // Act
            transport.Dispose();

            // Assert
            mockHttpClient.Verify(x => x.Dispose(), Times.Never);
        }
    }
}
